spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qma_db
    username: root
    password: kartik@mysql

  jpa:
    hibernate:
      ddl-auto: update

security:
  jwt:
    secret: ${jwt-secret-key}